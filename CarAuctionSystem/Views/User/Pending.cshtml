@model IEnumerable<AuctionViewModel>

@{
	ViewBag.Title = "Your pending auctions";
}

<h2 class="text-center">@ViewBag.Title</h2>

@if (!Model.Any())
{
	<p>You have no auctions awaiting approval.</p>
}
else
{

	<partial name="_PendingAuctionTablePartial"/>
}